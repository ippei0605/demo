function(doc) {
	var row = {
		"_id" : doc._id,
		"_rev" : doc._rev,
		"date" : doc.date,
		"hole" : doc.hole,
		"count" : doc.count,
		"club" : doc.club,
		"result" : doc.result,
		"latitude" : doc.latitude,
		"longitude" : doc.longitude
	};
	emit(doc.date, row);
}